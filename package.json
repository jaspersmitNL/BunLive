{
    "name": "bunlive-monorepo",
    "private": true,
    "workspaces": [
        "packages/*",
        "apps/*"
    ],
    "scripts": {
        "build": "turbo run build",
        "dev": "turbo run dev",
        "format": "prettier --write ."
    },
    "devDependencies": {
        "prettier": "^3.1.1"
    },
    "dependencies": {
        "turbo": "^1.11.2"
    }
}
